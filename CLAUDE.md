# CLAUDE.md

이 저장소의 규칙은 `AGENTS.md` 한 곳에서 관리한다. 아래 import로 그대로 불러온다.
**규칙을 고칠 때는 이 파일이 아니라 `AGENTS.md`를 수정하라.**

@AGENTS.md

---

## 작업 방식

### 끝내기 전에

`pnpm verify`(타입 + 린트 + 빌드)가 통과해야 작업이 끝난 것이다. 빌드가 깨진 채로 완료라고 보고하지 않는다.

에러 없이 통과했는데 경고가 남았다면, 그 경고가 이번 변경에서 생긴 것인지 원래 있던 것인지 구분해서 말한다.

### 확인하고 진행할 것

- **커밋·푸시**: 요청받았을 때만 한다. `main`/`dev`에 직접 커밋하지 않고 브랜치를 만든다
- **DB 마이그레이션**: SQL은 작성해서 보여주고, 실행은 사람이 한다
- **의존성 추가**: 먼저 표준 라이브러리 / 이미 설치된 패키지 / 플랫폼 기본 기능으로 되는지 확인한다
- **파일 삭제**: 참조가 정말 0건인지 확인한 뒤 지운다

### 환경변수

`.env.local`은 절대 커밋하지 않는다(`.gitignore`가 `.env*`를 덮는다). 값을 대화에 출력하지 않는다.

Vercel에서 값을 가져와야 하면 `npx vercel env pull`을 쓴다. 이 파일을 덮어쓰면 **로컬에만 있는 키가 날아간다** — 덮어쓰기 전에 기존 키 목록을 확인하라.

### 문서 동기화

코드를 바꾸면 관련 문서도 같은 커밋에서 갱신한다. 특히 `.specs/`는 과거에 코드와 크게 어긋난 적이 있다. 스펙에 적힌 API가 실제로 존재하는지 의심스러우면 **문서를 믿지 말고 코드를 확인하라.**

새로 발견한 문제를 즉석에서 고치기 어렵다면 `PLAN.md`에 항목으로 남긴다.
